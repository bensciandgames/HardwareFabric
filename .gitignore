# Secrets — never commit real environment files
backend/.env
frontend/.env.local
frontend/.env*.local
.env
.env.*
!.env.local.example
!.env*.example
*.pem
*.key
id_rsa*
*_rsa
*.pfx
secrets.*
credentials.*

# Workaround filenames used in place of real .env files (the remote-devices
# write bridge blocks writing literal .env/.env.local filenames) — these
# hold the same secret values and must never be committed either.
backend_env_values.txt
frontend_env_values.txt
*_env_values.txt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node / Next.js
node_modules/
frontend/.next/
frontend/out/
frontend/tsconfig.tsbuildinfo
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
*.swp
*.swo
*~
*.bak
*.tmp

# Stray staging artifact from initial transfer
/_incoming_update.tar.gz
